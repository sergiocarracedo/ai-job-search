# WeWorkRemotely CLI

A zero-dependency CLI for searching WeWorkRemotely's remote job listings.

## Usage

```bash
bun run src/cli.ts search --query "python" --format table
bun run src/cli.ts detail <id> --format plain
```

## Install

```bash
bun install
```

## Test

```bash
bun test
```
